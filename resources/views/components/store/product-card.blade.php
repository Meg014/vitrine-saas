<article class="group overflow-hidden rounded-2xl border bg-white"><a href="{{route('store.product',[$store,$product])}}"><div class="aspect-square bg-stone-100">@if($product->primaryImage)<img class="size-full object-cover transition group-hover:scale-105" src="{{Storage::url($product->primaryImage->path)}}" alt="{{$product->name}}">@else<div class="grid size-full place-items-center text-4xl">◇</div>@endif</div><div class="p-4"><p class="text-xs text-slate-500">{{$product->category?->name}}</p><h3 class="mt-1 font-semibold">{{$product->name}}</h3><div class="mt-3">@if($product->promotional_price)<span class="font-bold text-rose-600">R$ {{number_format($product->promotional_price/100,2,',','.')}}</span><del class="ml-2 text-sm text-slate-400">R$ {{number_format($product->base_price/100,2,',','.')}}</del>@else<span class="font-bold">R$ {{number_format($product->base_price/100,2,',','.')}}</span>@endif</div></div></a></article>
