<div><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm font-semibold text-violet-600">VENDAS</p><h1 class="text-3xl font-black">Pedidos</h1></div><a href="{{route('orders.settings')}}" class="rounded-xl border px-4 py-2">Configurar checkout</a></div><div class="mt-6 grid gap-3 sm:grid-cols-2"><input wire:model.live.debounce.300ms="search" placeholder="Número ou cliente"><select wire:model.live="status"><option value="">Todos os status</option>@foreach($statuses as $s)<option value="{{$s->value}}">{{$s->label()}}</option>@endforeach</select></div><div class="mt-6 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-left"><thead><tr class="border-b"><th class="p-4">Pedido</th><th>Cliente</th><th>Status</th><th>Pagamento</th><th>Total</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr class="border-b"><td class="p-4"><b>{{$order->number}}</b><small class="block text-slate-500">{{$order->created_at->format('d/m/Y H:i')}}</small></td><td>{{$order->customer_name}}</td><td>{{$order->status->label()}}</td><td>{{$order->payment_status->label()}}</td><td>R$ {{number_format($order->total/100,2,',','.')}}</td><td><a class="text-violet-700" href="{{route('orders.show',$order)}}">Abrir</a></td></tr>@empty<tr><td colspan="6" class="p-10 text-center">Nenhum pedido encontrado.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{$orders->links()}}</div></div>
