<div class="mx-auto max-w-5xl px-4 py-10"><h1 class="text-4xl font-black">Seu carrinho</h1><div class="mt-7 grid gap-6 lg:grid-cols-[1fr_320px]"><section class="space-y-4">@forelse($cart->items as $item)<article class="flex flex-wrap items-center gap-4 rounded-2xl border bg-white p-4"><div class="min-w-0 flex-1"><b>{{$item->product_name_snapshot}}</b><p class="text-sm text-slate-500">{{$item->variant_name_snapshot}} · {{$item->sku_snapshot}}</p><p class="mt-2 font-semibold">R$ {{number_format(($item->promotional_unit_price??$item->unit_price)/100,2,',','.')}}</p></div><input class="w-20" type="number" min="1" value="{{$item->quantity}}" wire:change="updateQuantity({{$item->id}},$event.target.value)"><b>R$ {{number_format($item->subtotal()/100,2,',','.')}}</b><button wire:click="remove({{$item->id}})" class="text-red-600">Remover</button></article>@empty<p class="rounded-2xl border border-dashed p-12 text-center">Seu carrinho está vazio.</p>@endforelse</section><aside class="h-fit rounded-2xl border bg-white p-5"><h2 class="text-xl font-bold">Resumo</h2><div class="mt-5 flex justify-between"><span>Subtotal</span><b>R$ {{number_format($cart->subtotal()/100,2,',','.')}}</b></div><p class="mt-5 text-sm text-slate-500">O cálculo de frete será disponibilizado no checkout.</p><a class="mt-5 block rounded-xl p-3 text-center font-bold text-white" style="background:var(--store-primary)" href="{{Auth::guard('customer')->check()?route('store.checkout',request()->route('store')):route('store.login',request()->route('store'))}}">Finalizar compra</a><a class="mt-3 block text-center text-sm" href="{{route('store.catalog',request()->route('store'))}}">Continuar comprando</a></aside></div></div>
